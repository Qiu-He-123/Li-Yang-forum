# 反爬防护说明（立洋社区）

企业级防护分五层，正常用户全程无感；被攻击时按"验证码 → 限流 → 封禁 → 限速"逐层拦截。

## 1. 图形验证码（应用层，新增）

基于 GitHub 开源库 **fast_captcha**（MIT，<https://github.com/wu-clan/fast_captcha>），
答案只存服务端（`captcha_tickets` 表），一次性、5 分钟过期、绑定 IP。

验证码强制场景：
- **注册** `/auth/register`：必须携带验证码
- **登录** `/auth/login`：必须携带验证码（防密码爆破）
- **下载** `/api/app-download`：必须先过验证码换取一次性下载令牌
  （`POST /api/app-download/token`，令牌 2 分钟过期、一次性、绑定 IP）
- **高频访问**：每 IP 每分钟 GET /api 超过 90 次 → 弹验证码挑战
  （`code=-401`），验证一次签发 10 分钟通行证 Cookie（HMAC 签名 + 绑定 IP）

验证码接口：
- `GET /api/captcha`：获取验证码（每 IP 每分钟最多 10 张）
- `POST /api/captcha/verify`：校验并签发挑战通行证

错误码：`-401` 需要验证码 / `-402` 验证码错误 / `-403` 验证码过期。

## 2. 应用层限流（FastAPI）

- **写接口中间件**（`backend/app/main.py` `_WRITE_LIMITS`）：
  POST/PUT/PATCH/DELETE 按 IP 限流（60 秒窗口，复用 `rate_limits` 表）。
  发帖 5 次/分、评论 10 次/分、签到 2 次/分、AI 5 次/分、搜索 30 次/分，其余写接口默认 30 次/分。
- **注册硬上限**：每 IP 每天最多 5 个账号（24 小时窗口），防止打码平台换 IP 刷号。
- **登录/注册**：每 IP 每分钟 10 次 + 登录失败 10 次锁 30 分钟（持久化）。
- **下载硬上限**：每 IP 每小时最多 5 次、每天最多 20 次（`app_download.py`）。
- **高频访问挑战**：每 IP 每分钟 GET /api 超过 90 次触发验证码（`main.py` `api_read_challenge`）。

## 3. Nginx 层（frontend 容器内）

- **nginx-ultimate-bad-bot-blocker**：坏 UA / 坏来源 / 坏 IP 直接 444 断开，可疑 bot 自动限速。
  - 上游：<https://github.com/mitchellkrogza/nginx-ultimate-bad-bot-blocker>（MIT License）
  - 已 vendored 到 `frontend/ngxblocker/`，构建镜像时复制进 `/etc/nginx`。
- **API 总限流**：每 IP 每分钟最多 120 次 `/api` 请求（`nginx-ratelimit.conf` `api_limit`）。
- **APK 下载专用**：`location = /api/app-download` 独立限流——
  每 IP 每分钟最多 6 次（`download_limit`）+ 同 IP 最多 2 个并发连接（`download_conn`）
  + 单连接限速 512KB/s（`limit_rate`）。

## 4. 服务器层（宿主机 fail2ban）

- 配置见 `deploy/fail2ban/`（含部署说明）。
- `docker-compose.yml` 已把 frontend 容器的 `/var/log/nginx` 挂载到宿主机 `logs/nginx/`。
- `nginx-limit-req` 过滤器已修正为匹配实际区名 `api_limit download_limit`（原为 wp-login，失效）。

## 5. 云端层（生产建议，仓库内无法实现）

- 接入 CDN / 云 WAF（流量清洗、DDoS 防护、带宽告警）。
- 域名加 HTTPS（HSTS），安全组只放行必要端口。

## 调参位置

- 验证码限制：`backend/app/services/captcha_service.py`
- 高频挑战阈值：`backend/app/main.py` 的 `READ_CHALLENGE_LIMIT`
- 注册/下载限额：`backend/app/services/auth_service.py`、`backend/app/api/routes/app_download.py`
- Nginx 频率：`frontend/nginx-ratelimit.conf`（`rate=`）与 `frontend/nginx.conf`（`burst`/`limit_rate`/`limit_conn`）
- fail2ban 封禁力度：`deploy/fail2ban/jail.local`

## 部署提醒

- 数据库迁移：`alembic upgrade head`（新增 `captcha_tickets`、`download_tokens` 表）。
- 后端依赖：`pip install -r requirements.txt`（新增 `fast-captcha`）。
- 前端需重新构建（`npm run build`），登录/注册/下载流程已接入验证码。

---

# 安全审计修复清单（2026-08-13 第二轮）

按 GitHub 安全实践逐项修复，已实测验证。

## 高危漏洞（已修复）

1. **任意文件读取（源码泄露）** —— SPA 兜底路由用用户输入拼路径，
   `/C:/Windows/win.ini`、`//etc/passwd`、`/api//app/app/main.py` 等可读出
   系统文件和后端源码（含 `任务书.txt` 等内部文档）。
   修复：`backend/app/main.py` `serve_spa` 拒绝绝对路径/`..` 穿越，
   `resolve()` + `is_relative_to()` 双保险，只允许 dist 目录内文件。
2. **X-Forwarded-For 伪造绕过限流** —— 旧 `extract_ip` 直接信任客户端
   传入的 XFF，攻击者换一个头就能重置所有按 IP 的限流。
   修复：`backend/app/api/deps.py` 只信任 Nginx 覆写的 `X-Real-IP`；
   `frontend/nginx.conf` 各反代 location 用 `$remote_addr` 覆写
   `X-Forwarded-For`，客户端伪造无效。
3. **requests 2.32.3 两个已公开漏洞**（PYSEC-2026-1872 / PYSEC-2026-2275）
   —— 已升级到 2.33.0，pip-audit 复扫无已知漏洞。

## 高危配置（已修复）

4. **生产环境 API 文档暴露** —— `/docs` `/redoc` `/openapi.json` 泄露全部
   接口结构。修复：`backend/app/main.py` 在 `ENV != dev` 时关闭三个文档路由。
5. **默认弱口令** —— MySQL 默认密码 `lycommunity`、MinIO 默认
   `minioadmin/minioadmin`。修复：生产模式启动检查，检测到默认口令直接
   拒绝启动并提示改密。
6. **容器以 root 运行** —— `backend/Dockerfile` 新增非 root 用户
   `appuser`（uid 10001），并 `COPY static` 保证 APK 进镜像。
7. **内部文档进镜像** —— 新增 `backend/.dockerignore`，`任务书.txt`、测试
   脚本、`.env`、数据库文件一律不进容器。

## 中危（已修复）

8. **音频上传无魔数校验** —— 伪装 content_type 可上传任意文件。
   修复：`images.py` 新增 `_detect_audio_type()`（webm/ogg/amr/mp3/mp4/aac
   魔数校验），不匹配一律 400。
9. **私密图片路径穿越** —— `read_private` 拼接文件名前未校验目录。
   修复：`storage_service.py` 增加 `resolve()` + `is_relative_to()` 约束。
10. **WebSocket 消息洪泛** —— 匹配聊天消息无长度上限。修复：
    `match_service.handle_match_chat` 截断至 2000 字符。
11. **限流表无限膨胀** —— 新增 `_start_security_cleanup` 后台任务，每 10
    分钟清理 48 小时前的限流计数、30 分钟前的验证码票据、10 分钟前的下载令牌。
12. **前端遗留直链下载** —— `platform.ts`、`NotificationSettings.vue`
    原先直接跳 `/api/app-download`（绕过新验证码流程且会 404），已改为
    与下载按钮一致的验证码换令牌流程。
13. **Nginx 信息泄露 / 连接洪泛** —— `server_tokens off` 隐藏版本号；
    全局同 IP 20 并发连接限制；`limit_req_status`/`limit_conn_status` 统一 429。

## 第三轮：评论游客加密（2026-08-13）

**背景**：评论区对未登录用户做了前端模糊（`CommentList.vue` 的 `is-guest`
样式），但明文一直躺在 DOM 和网络响应里，用开发者工具删掉模糊即可阅读。

**修复**（服务端加密，前端模糊保留）：
- `backend/app/services/comment_service.py` 的 `comment_dict` 新增
  `hide_for_guest`：游客请求评论列表时，评论内容返回 `🔒 登录后查看`，
  昵称返回 `同学`，头像返回默认头像，徽章置空。
- 明文**根本不返回给浏览器**——删模糊、看网络包、扒 DOM 都拿不到真实内容。
- 登录用户不受影响，照常返回完整明文。

**原理说明**：真正的"客户端加密"不可行——任何随页面下发给浏览器的密钥都能被
提取。业界标准做法就是"未登录不发明文"，本修复即此方案。

**验证**：同一批评论，游客收到占位符（内容/昵称/头像/徽章全部脱敏），
登录用户收到完整明文，两条链路均已实测。

## 验证结果

- 8 条路径穿越攻击向量（含 Windows/Linux 绝对路径、编码 `..`、`/api//` 变体）全部拦截
- 伪造 XFF 不再影响限流键（单元测试 + 裸 socket 双验证）
- pip-audit 无已知漏洞
- 音频伪装文件（HTML/纯文本）被拒，真实 webm/ogg/mp3/m4a 全部通过
- 前端 `vue-tsc` + `vite build` 通过

## 第四轮：安全审计修复（2026-08-13）

### 致命（已修复）

1. **JWT 无类型声明 → 用户 token 可提权为管理员**
   - `app/core/security.py`：payload 增加 `"type"`（access/refresh/admin），
     `decode_token` 支持 `expected_type` 强校验。
   - 全部 9 处 `decode_token` 调用点已按类型收紧（deps/refresh/feedback/images/ws）。
   - 效果：把用户 access_token 塞进 `admin_token` Cookie 直接返回 401。
2. **真实凭证已泄出（工作区 + git 历史）**
   - `backend/_tmp_login.json`（登录凭证）、`cookies.txt`、`backend/cookies.txt`、
     `backend/cookies.csv`（有效会话 JWT）已从工作区删除，git 历史已用
     filter-branch 全部重写并 gc 回收（含 refs/original 备份引用）。
   - `.gitignore` 新增 `*_tmp_*.json`、`keystore-password.txt`、`*.key`、`*.pem`。
   - **需要人工执行**：
     a. 到 GitHub 撤销并轮换 `backend/.env` 里的 `GITHUB_TOKEN`（PAT）
     b. 轮换生产 `JWT_SECRET`（使泄露的会话 JWT 全部失效）
     c. 重设 `_tmp_login.json` 里的测试账号密码
     d. `git push --force --all origin`（远程仓库同步清理后的历史）

### 高危（已修复）

3. **X-Real-IP 伪造绕过限流** —— `deps.py extract_ip` 改为：仅
   `ENV != dev`（Nginx 强制覆写 X-Real-IP 且后端不对外暴露）才信任代理头；
   dev 直跑 uvicorn 一律用 TCP 对端 IP，伪造头无效。
4. **管理员登录无图形验证码** —— `admin_login` 接入 `verify_captcha`，
   前端管理登录页 [Login.vue](frontend/src/views/Admin/Login.vue) 已加验证码输入。

### 中危（已修复）

5. **AI/DeepSeek base_url SSRF** —— 新增 `app/services/url_safety.py`：
   解析目标 host 的全部 IP，命中内网/回环/链路本地/保留/组播地址或解析失败一律拒绝。
   已接入 `ai_service._chat`、`deepseek_audit`、`deepseek test_connection`。
6. **登录时间侧信道枚举账号** —— 用户不存在时同样执行一次 bcrypt 校验
   （`auth_service._DUMMY_BCRYPT_HASH`），响应时间不再泄露账号存在性。
7. **验证码过弱（4 位）** —— 升级为 5 位 + 干扰点（`captcha_service.py`），
   前端验证码图片宽度同步调整。若要更高强度，建议接入 Cloudflare Turnstile。

### 低危（已处理/说明）

- docker-compose 默认弱口令：新增 `deploy/.env.example` 强口令模板；
  后端 prod 启动强校验兜底（默认口令直接拒绝启动）。
- `images.py` Content-Length 校验可被 chunked 绕过：`_read_limited` 分块读取
  已兜底限制内存，影响有限，无需额外修复。
- bandit B608（alembic_helper.py 固定字符串）确认为误报，无需处理。

## 第五轮：中低危审计修复（2026-08-13）

### 中危（已修复）

1. **PII 手机号泄露** —— `user_service.profile()` 原来对任何人返回完整 `phone`。
   修复：只有查看本人资料才返回完整手机号，其余一律脱敏为 `138****1234`。
2. **头像/背景图外链追踪** —— `ProfileUpdate` 原来只限长 500 不校验协议，
   可被设成 `http://attacker.com/t.png?uid=xxx` 去匿名化访问者。
   修复：只允许站内 `/uploads/`、`/minio/` 路径，外部 http(s)/javascript:/data: 一律拒绝。
3. **SSRF 加固** —— `url_safety.validate_public_url` 升级为仅允许 `https://`
   且解析到公网 IP（原有内网 IP 黑名单保留）。
4. **X-Real-IP 信任残留** —— `deps.extract_ip` 改为：仅当直连来源属于
   `trusted_proxies` 白名单（默认 127.0.0.1/::1/docker/内网网段）且生产模式
   才信任 X-Real-IP；后端端口被直接暴露时伪造头无效。
5. **吧主任命 role 未校验** —— `circle_apply.add_circle_admin` 现在
   role 仅允许 admin/owner，且禁止任命他人为 owner（防越权提权）。
6. **ip=None 跳过限流** —— `check_ip_rate_limit` 对无客户端地址的请求
   归入 `ip:unknown:*` 公共桶，不再放行。
7. **current_user_optional 不拦封号** —— 与 `optional_user` 对齐：
   封号用户访问浏览接口一律 403。

### 已核实无需修改

- **home_marquee 字幕 XSS** —— 前端用 `{{ }}` 插值渲染（非 v-html），天然转义，无 XSS。

### 验证结果

- 本人查看完整手机号 / 他人查看 `138****1234`
- 外链、javascript:、data: 头像 URL 全部拒绝，站内路径正常
- http:// 公网地址被 SSRF 拒绝，https:// 公网正常，内网/元数据拒绝
- dev 环境伪造 X-Real-IP 无效；prod 仅可信反代来源生效
- ip=None 写入 unknown 限流桶；owner 任命被拒；封号用户被 current_user_optional 拦截

## 第六轮：性能与流量优化（2026-08-13）

### 1. Nginx GZip 压缩（最大头，实测省 68%）

`frontend/nginx.conf` 新增 gzip 配置（level 6，JS/CSS/JSON/SVG 等）。
实测全部构建产物：**2,121,101 B → 681,024 B（-68%）**。
后端 GZipMiddleware 只压缩 FastAPI 返回的 JSON，静态资源由 Nginx 直接
吐出，此前完全未压缩。

### 2. 静态资源缓存

- `/assets/`（带 hash 的 JS/CSS）：`Cache-Control: public, max-age=31536000, immutable`，永久缓存
- `/index.html`：`no-cache, no-store`，发版即时生效
- `/minio/` 与 `/uploads/`（uuid 文件名不可变）：缓存 30 天
- 新增 `frontend/nginx-security-headers.conf` 片段：子 location 设置缓存头会
  中断 add_header 继承，带缓存头的 location 通过 include 重复注入安全响应头

### 3. 图片输出改 WebP（同质量再省约 30%）

`backend/app/api/routes/images.py`：压缩图与缩略图统一输出 WebP（已验证
魔数 `RIFF....WEBP`），GIF 动图原样保留。头像/背景/帖子/聊天/活动图全部覆盖。

### 4. 轮询降频（后端压力）

- MatchView：匹配人数 5s→10s、会话同步 5s→10s、等待匹配 3s→6s（WS 兜底）
- 首页统计 / 漂流瓶统计 / 聊天消息 / 通知未读数：30s→60s（未读数已有 WS 实时触发）

### 5. 图片懒加载（已具备，核实无需改）

首页/圈子瀑布流封面与 PostImages 已带 `loading="lazy"`（PostImages 另有
`decoding="async"`），首屏只加载可见部分。

### 其他（核实）

- dayjs（53KB）、marked（73KB）已是独立懒加载 chunk，首屏不背载，无需再拆
- terser 换 esbuild：仅省 3-5% 且拖慢构建，维持现状
