# 反爬防护说明（立洋社区）

三层防护，正常用户全程无验证码、无感知。

## 1. Nginx 层（frontend 容器内）

- **nginx-ultimate-bad-bot-blocker**：坏 UA / 坏来源 / 坏 IP 直接 444 断开，可疑 bot 自动限速。
  - 上游：<https://github.com/mitchellkrogza/nginx-ultimate-bad-bot-blocker>（MIT License）
  - 已 vendored 到 `frontend/ngxblocker/`，构建镜像时复制进 `/etc/nginx`。
  - 更新名单：拉取上游仓库，替换 `frontend/ngxblocker/conf.d` 与 `frontend/ngxblocker/bots.d` 后重建镜像。
- **API 限流**：每 IP 每分钟最多 120 次 `/api` 请求（`frontend/nginx-ratelimit.conf`，在 `location /api/` 内生效）。

## 2. 服务器层（宿主机 fail2ban）

- 配置见 `deploy/fail2ban/`（含部署说明）。
- `deploy/docker-compose.yml` 已把 frontend 容器的 `/var/log/nginx` 挂载到宿主机 `logs/nginx/`，fail2ban 直接读宿主机日志封禁高频 IP。

## 3. 应用层（FastAPI）

- `backend/app/main.py` 新增写接口限流中间件：POST/PUT/PATCH/DELETE 按 IP 限流（60 秒窗口，复用 `rate_limits` 表）。
  - 发帖 5 次/分、评论 10 次/分、签到 2 次/分、AI 接口 5 次/分、搜索 30 次/分等，其余写接口默认 30 次/分。
  - 超限返回 `code=-400`（`RATE_LIMITED`，msg：操作太频繁，请稍后再试）。
- 原有登录/注册/改密 IP 限流（10 次/分）保留不变。

## 调参位置

- Nginx 频率：`frontend/nginx-ratelimit.conf`（`rate=`）与 `frontend/nginx.conf` 的 `burst`。
- 写接口限额：`backend/app/main.py` 的 `_WRITE_LIMITS`。
- fail2ban 封禁力度：`deploy/fail2ban/jail.local`。
