# 立洋社区 更新指南

每次从 GitHub 拉取新版后，**只需要双击 `重启服务器.bat`**，其余全部自动完成。
本文件说明更新流程、自检内容与常见问题。

---

## 一、更新流程（三步）

1. **拉取最新代码**（任选其一）
   - 有 git 环境：在仓库根目录执行 `git pull`
   - 或直接把 GitHub 上的仓库下载覆盖（保留下面「更新前先备份」里的文件）

2. **双击 `重启服务器.bat`**（或首次部署用 `启动立洋社区.bat`）

3. 等待窗口提示 **「自检全部通过 ✓」**，向导打开后选账号 → 启动服务器

> 自检会自动完成：依赖缺失自动安装 / 版本低了自动升级 / 前端自动构建 /
> 数据库自动迁移 / 旧服务器进程自动清理。**全程不需要手动操作**。

## 二、更新前先备份（防止被覆盖/丢失）

以下文件是**本机私有配置**，git 更新不会动它们，但覆盖式下载时要留意：

| 文件 | 作用 |
|---|---|
| `微信同步客户端/config.json` | 设备令牌、数据目录、账号 |
| `微信同步客户端/账号配置/` | 各账号的数据库/图片密钥（删了要重抓！） |
| `backend/.env` | 数据库连接、JWT 密钥等 |
| `server_config.json` | 对外开放开关、端口 |

## 三、自检做了什么（及提速原理）

`backend/scripts/bootstrap.py` 顺序自检：

1. **Python**：优先挑带 tkinter 的版本（图形向导需要）；全都没有时自动下载官方安装器补装 tcl/tk
2. **虚拟环境**：缺则创建；缺 tkinter 则用带 tkinter 的 Python 自动重建
3. **后端依赖**：`requirements.txt` + 密钥工具依赖，缺失/升级自动 pip 安装（默认源失败自动切清华镜像）
4. **.env**：缺则从 `.env.example` 复制
5. **前端依赖**：`npm install`（默认源失败自动切 npmmirror）
6. **前端构建**：`npm run build` 生产产物
7. **数据库迁移**：`alembic upgrade head`

**提速原理（不影响质量）**：上次安装成功后写入标记文件
（`.venv/.deps_ok`、`node_modules/.npm_ok`）。依赖清单（requirements /
package.json）自上次安装后**没变过**就跳过 pip/npm 检查（约 1 秒完成自检）；
**有变化**（比如 git pull 更新了清单）才重新安装，质量不缩水。

前端构建也做了加速：自动构建用 **`vite build` 快速模式**（跳过 vue-tsc 类型检查，
约 10-15 秒；需要完整类型检查可手动 `cd frontend && npm run build`），并且用
**源码内容哈希**判断——git pull 只改了文件时间没改内容时**直接跳过构建**，
不再每次全量重编。

## 四、常见问题排查

| 现象 | 原因 | 处理 |
|---|---|---|
| bat 一闪而过 | 老版本 bat 里带中文/cmd 解析乱码 | 更新到新版（纯 ASCII bat）；仍闪退就把窗口内容截图发来 |
| `No module named 'tkinter'` | Python 没装 tcl/tk 组件 | 已自动处理：优先换带 tkinter 的 Python / 自动下载补装；实在不行自动无界面启动 |
| 向导打不开但服务器能跑 | 同上 | 无界面模式照常启动；密钥可用命令行工具配置（见启动失败提示） |
| 端口被占用 | 8000 被别的程序用了 | 改 `server_config.json` 的 `port`（如 8001）后重启 |
| 其他机器/外网访问不了 | uvicorn 默认只绑 127.0.0.1 | 把 `server_config.json` 的 `bind_host` 改成 `0.0.0.0` 后重启（会自动放行防火墙、打印访问地址） |
| `no such table` 报错 | 老版本从仓库根目录启动导致建了空库 | 更新到新版（数据库路径已锚定 backend 目录）；如已有根目录空库可删除 |
| 图片密钥"错误也能通过" | 老版本校验逻辑把 V1 格式当有效样本 | 更新到新版（只认 V2 + PIL 真实解码 + 多张交叉验证） |
| 同步不生效 | 微信没刷新朋友圈 / 没开自动同步 | 运行根目录 `诊断同步.py` 看链路哪一步 ❌ |

## 五、为什么脚本是"纯 ASCII"

cmd 对 bat 里的中文是按系统码页逐字节解析的，UTF-8 中文会让 bat 命令被拆碎、
窗口秒退。因此 bat 只保留英文提示，中文全部交给 Python 子进程输出（UTF-8 强制）。
**不要往 bat 里加中文**，提示文案请加在 Python 脚本里。

## 六、服务器访问配置（server_config.json）

```json
{
  "bind_host": "127.0.0.1",
  "port": 8000,
  "open_browser": true
}
```

- `127.0.0.1`：仅本机（配路由侠等内网穿透用）
- `0.0.0.0`：对外开放，局域网/公网机器通过 `http://本机IP:端口` 直接访问
- 改完保存 → 重新双击 `重启服务器.bat` 生效
