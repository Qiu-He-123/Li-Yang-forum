{
  "bind_host": "127.0.0.1",
  "port": 8000,
  "open_browser": true,
  "_说明": "bind_host: 127.0.0.1 = 仅本机访问（默认，配合路由侠等内网穿透用）；0.0.0.0 = 对外开放，局域网/公网机器可通过 http://本机IP:端口 直接访问。port: 服务端口，被占用可改 8001 等。open_browser: 启动后是否自动打开浏览器。修改保存后，重新运行 重启服务器.bat 生效。"
}
