# 2026-07-27 工作日志

## 1. 图片上传速度修复 + 进度条
- **后端**: `storage_service.py` 新增 `upload_image_async()` 异步上传方法，使用 aiofiles 避免阻塞事件循环；MinIO 上传用 `run_in_executor` 放到线程池；MinIO 连接检查增加 3s 超时
- **后端**: `images.py` 新增缩略图生成（Pillow, 400x400 JPEG），原图+缩略图并行上传；返回增加 `thumb_url` 字段
- **后端**: `requirements.txt` 新增 `aiofiles` 和 `Pillow`
- **前端**: `api/image.ts` 新增 `onUploadProgress` 进度回调
- **前端**: `PostEditor.vue` 新增上传进度条 UI（占位 cell + 百分比进度条 + 旋转图标）；每张图片独立跟踪进度
- **前端**: `types/api.ts` ImageUploadResult 增加 `thumb_url` 字段

## 2. 帖子发布功能检查
- `is_anonymous`（匿名）、`is_original`（原创）、`is_public`（公开可见）三个字段后端均正确保存和返回
- 前端 PostEditor 正确传递三个字段到 API
- 前端 `Post` 类型补上缺失的 `is_public` 字段

## 3. 好友系统
- **后端**: `entities.py` Message 表扩展 `msg_type` + `conversation_id` 字段；新增 `FriendRequest` 表
- **后端**: `message_service.py` 完整好友+私信业务逻辑（发送/接受/拒绝好友请求、好友列表、搜索用户、发送/获取私信、会话列表）
- **后端**: `routes/messages.py` 完整路由（/friends/*, /messages/*）
- **后端**: `main.py` 注册 messages 路由
- **后端**: Alembic 迁移 `0004_add_friend_request_and_message_fields.py`
- **前端**: `api/friend.ts` 完整好友+私信 API 模块
- **前端**: `FriendsView.vue` 通讯录页面（好友列表 + 好友请求 + 搜索添加）
- **前端**: `ChatView.vue` 高仿微信聊天界面（绿色气泡/白色气泡、时间分组、3秒轮询）
- **前端**: router 新增 `/friends` 和 `/chat/:id` 路由

## 4. 消息红点系统
- **前端**: `Notifications.vue` 全面重设计：双Tab（互动通知/私信）、分类入口卡片红点替代数字badge、好友会话列表
- **前端**: `BottomTabBar.vue` 消息 Tab badge 改为红点样式
- **前端**: `AppHeader.vue` 通知 badge 改为红点 + 调整 CSS
- **前端**: `stores/notification.ts` 新增 `byType` 按类型未读计数
- **后端**: `notification_service.py` unread_count 返回 `by_type` 各类型未读数
