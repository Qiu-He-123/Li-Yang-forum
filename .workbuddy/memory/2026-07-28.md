# 2026-07-28

- 修复 frontend/src/views/Banned.vue：文件被截断只剩 `<script setup` 14 个字符，导致 vite:vue 报 "Unexpected EOF in tag"（Banned.vue:1:14）。项目无 git 历史可恢复，根据 router/index.ts、api/user.ts（fetchBanStatus / createAppeal / fetchMyAppeals）、stores/session.ts 的封号流程重建了完整页面：展示封禁原因/期限/违规次数、提交申诉、申诉记录列表（pending/approved/rejected 状态标签）、退出登录；封禁解除时自动清除 localStorage 标记并回首页。已用项目内 @vue/compiler-sfc 验证编译通过。

- 修复「账号封禁后仍可使用，未立即显示封禁界面」：根因是 /auth/me 不返回 ban_info、validateSession 不检测封禁，导致刷新页面/切换页面时检测不到封号（只有写操作返回 -301 才跳转）。改动：①backend app/api/routes/auth.py 的 /auth/me 增加 ban_info（复用 user_service.get_ban_status）；②frontend stores/session.ts validateSession 检测 ban_info.is_banned → 设 localStorage banned 标记 + router.replace 到 /banned；解封则清标记并从 /banned 回首页；③App.vue 增加登录态下每 45s 轮询 validateSession，覆盖「被封后停留在页面不操作」的 idle 场景。后端 16 个 auth 测试全过；admin 的 6 个失败是预存在（admin 列表接口返回分页对象而测试 assert isinstance list，与本次改动无关）。注意：后端 /auth/me 改动需重启后端生效。

- 修复三个问题：①发帖后弹窗显示两次且文案应为"审核中"：PostEditor.vue publish() 弹"已发布" + emit('published') 触发 PostCreate.vue onPublished() 再弹"发布成功"。改 PostEditor 文案为"发布成功，内容审核中"，删 PostCreate 的重复 toast。②个人主页帖子列表不显示状态标签：后端 user_service.py user_posts 返回精简自定义字典（缺 ai_status/reject_reason/title），导致 AiStatusBadge 不渲染。改为用 post_dict(p) 序列化（与 my_favorite_posts/my_liked_posts 一致）。③消息红点需点击后才显示：通知轮询仅在 AppHeader.vue（只挂载在部分页面）中启动。将通知轮询逻辑提升到 App.vue 全局管理（onMounted 启动 + watch session.userId + route.path 刷新），AppHeader 仅保留 store 读取。前端 SFC 编译全过；后端 11 个 user 测试全过（test_post::test_update_post_by_author 失败是预存在 bug：更新帖子内容未持久化，与本次改动无关）。后端 user_service 改动需重启后端生效。

- 重做封禁拦截（用户要求改为后端统一拦截方案）：在 backend/app/api/deps.py 的 current_user 依赖中统一检查封禁状态——每次请求都根据 token 解析用户，若被封禁直接 raise -301，阻止所有非豁免接口。新增 _is_user_banned(user) 无副作用判断函数（ban_until 未过期 或 is_active=False）。新增 current_user_allow_banned 依赖供豁免接口使用。optional_user 也加封禁拦截（封号用户连公开浏览都被阻止）。豁免接口（改用 current_user_allow_banned）：/auth/me（会话校验+返回 ban_info）、/auth/logout（封禁页退出登录）、/users/me/ban-status（查看封号信息）、/users/me/appeals GET+POST（查看/提交申诉）。登录/注册/发验证码/refresh 不走 current_user，天然豁免。前端无需额外改动——http 拦截器已有 -301 → handleBannedRedirect 跳转 /banned；validateSession 的 ban_info 检测 + App.vue 45s 轮询作为补充。端到端验证通过：封禁用户调普通接口返回 -301，调豁免接口正常。后端 68 个测试通过（2 个预存在失败：test_update_post_by_author 和 test_post_detail_repairs_stale_comment_count，均与封禁无关）。后端改动需重启后端生效。
